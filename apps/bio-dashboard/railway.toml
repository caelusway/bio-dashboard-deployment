[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/bio-dashboard/Dockerfile"

[deploy]
startCommand = ""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 100
